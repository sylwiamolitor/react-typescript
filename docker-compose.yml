version: '3.7'

services:

  frontend:
    container_name: container
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/react-typescript'
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
